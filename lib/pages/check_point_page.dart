import 'package:flutter/material.dart';

class CheckPointPage extends StatelessWidget {
  const CheckPointPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Ceckpoint Page');
  }
}
